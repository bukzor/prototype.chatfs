[project]
name = "chatfs-cli"
version = "0.1.0"
description = "Lazy filesystem for chat conversations (claude.ai, ChatGPT)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-typed-json",
]

[project.optional-dependencies]
dev = [
    "basedpyright-as-pyright",
    "pytest",
    "pytest-pyright",
]

# One command per pipeline leaf, plus a bare-noun driver where a noun's
# stages compose into a single default: kebab name == module path, per
# design.kb/040-design.kb/cli-command-shape.md. The bare `chatfs` name is
# reserved for the future Rust dispatcher (package-division.md).
[project.scripts]
chatfs-claude-conversation-url-browse = "chatfs.provider.claude.conversation.url_browse:main"
chatfs-claude-conversation-url-render = "chatfs.provider.claude.conversation.url_render:main"
chatfs-claude-conversation-url-trash = "chatfs.provider.claude.conversation.url_trash:main"
chatfs-claude-conversation-path-browse = "chatfs.provider.claude.conversation.path_browse:main"
chatfs-claude-conversation-path-render = "chatfs.provider.claude.conversation.path_render:main"
chatfs-claude-conversation-render = "chatfs.provider.claude.conversation.render:main"
chatfs-claude-conversation-splat = "chatfs.provider.claude.conversation.splat:main"
chatfs-claude-index = "chatfs.provider.claude.index.__main__:main"
chatfs-claude-index-browse = "chatfs.provider.claude.index.browse:main"
chatfs-claude-index-splat = "chatfs.provider.claude.index.splat:main"
chatfs-chatgpt-conversation-url-browse = "chatfs.provider.chatgpt.conversation.url_browse:main"
chatfs-chatgpt-conversation-url-render = "chatfs.provider.chatgpt.conversation.url_render:main"
chatfs-chatgpt-conversation-path-browse = "chatfs.provider.chatgpt.conversation.path_browse:main"
chatfs-chatgpt-conversation-path-render = "chatfs.provider.chatgpt.conversation.path_render:main"
chatfs-chatgpt-conversation-render = "chatfs.provider.chatgpt.conversation.render:main"
chatfs-chatgpt-conversation-splat = "chatfs.provider.chatgpt.conversation.splat:main"
chatfs-chatgpt-index = "chatfs.provider.chatgpt.index.__main__:main"
chatfs-chatgpt-index-browse = "chatfs.provider.chatgpt.index.browse:main"
chatfs-chatgpt-index-splat = "chatfs.provider.chatgpt.index.splat:main"
chatfs-aistudio-conversation-url-browse = "chatfs.provider.aistudio.conversation.url_browse:main"
chatfs-aistudio-conversation-url-render = "chatfs.provider.aistudio.conversation.url_render:main"
chatfs-aistudio-conversation-path-browse = "chatfs.provider.aistudio.conversation.path_browse:main"
chatfs-aistudio-conversation-path-render = "chatfs.provider.aistudio.conversation.path_render:main"
chatfs-aistudio-conversation-render = "chatfs.provider.aistudio.conversation.render:main"
chatfs-aistudio-conversation-splat = "chatfs.provider.aistudio.conversation.splat:main"
chatfs-aistudio-conversation-massage-json = "chatfs.provider.aistudio.conversation.massage_json:main"
chatfs-aistudio-index = "chatfs.provider.aistudio.index.__main__:main"
chatfs-aistudio-index-browse = "chatfs.provider.aistudio.index.browse:main"
chatfs-aistudio-index-splat = "chatfs.provider.aistudio.index.splat:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lib/chatfs"]

[tool.pytest.ini_options]
python_classes = ["Describe*", "When*", "Test*"]
python_functions = ["it_*", "test_*"]

[tool.uv.sources]
basedpyright-as-pyright = { git = "https://github.com/bukzor/basedpyright-as-pyright" }

# Scoped to direct invocations rooted at this package (e.g. the
# typesafety/ pyrightconfig.json's `extends`); the repo-root
# `basedpyright .` sweep resolves its own [tool.pyright] instead and
# never reads this one.
[tool.pyright]
typeCheckingMode = "strict"
analyzeUnannotatedFunctions = true
reportIncompatibleMethodOverride = "error"
pythonVersion = "3.13"
enableTypeIgnoreComments = true
reportPrivateUsage = "warning"
reportConstantRedefinition = "error"
reportIncompatibleVariableOverride = "error"
reportMissingImports = "error"
reportMissingTypeStubs = false
exclude = [".venv", "__pycache__", "typesafety"]
