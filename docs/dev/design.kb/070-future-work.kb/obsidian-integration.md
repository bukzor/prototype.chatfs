---
why:
  - unix-composability
---

# Obsidian Integration

Verify markdown + YAML frontmatter compatibility with Obsidian. Test wikilinks
between conversations and graph view support.

May be free if the output format is already compatible.
