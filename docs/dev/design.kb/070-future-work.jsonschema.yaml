$schema: "http://json-schema.org/draft-07/schema#"
description: Schema for future-work entries in 070-future-work.kb/
type: object
additionalProperties: false
required:
  - why
properties:
  why:
    description: Mission/goal/requirement this future work would serve if pursued
    type: array
    items:
      type: string
    minItems: 1
