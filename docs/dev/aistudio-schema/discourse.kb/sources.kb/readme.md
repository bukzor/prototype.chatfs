---
kind: report
title: "AI Studio schema extraction — README"
url: "../README.md"
tags: [aistudio, schema, provenance]
---

The toolkit's own `README.md` (`docs/dev/aistudio-schema/README.md`).
Origin of nearly every claim in this graph. Describes motivation, why the
approach works, the source capture, the pipeline, the accessor grammar, and
the (explicitly partial) primitive legend.

Self-authored project doc, so it reports intent and observed bundle facts;
it is not independent verification that the extraction is correct or complete.
