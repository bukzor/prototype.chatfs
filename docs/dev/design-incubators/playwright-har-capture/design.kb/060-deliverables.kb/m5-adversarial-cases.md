---
why:
  - reusable-capture-components
---

# M5 — Adversarial Cases

**Out of scope.** The listed cases are either trivial at the HAR capture layer
(slow responses just produce different timestamps; cancelled captures produce
no output) or belong to downstream pipeline stages (encoding handling, large
payloads).
